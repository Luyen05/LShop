<div class="container">
    <div class="row">
        {{#if sidebar}}
            <div class="col-md-3">
                {{>sidebar}}
            </div>
        {{/if}}
        <div class="col-md-9">
            <h1>Danh sách sản phẩm</h1>
            <div class="row">
                {{#each products}}
                    {{>product-item}}
                {{/each}}
            </div>
        </div>
    </div>
</div>
